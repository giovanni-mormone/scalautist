sqlserver = {
  profile = "slick.jdbc.SQLServerProfile$"
  db {
    host = "scalautistserver.database.windows.net"
    port = 1433
    databaseName = "scalautisttest"
    url = "jdbc:sqlserver://"${sqlserver.db.host}":"${sqlserver.db.port}";databaseName="${sqlserver.db.databaseName}";password="${sqlserver.db.password}
    user = "teamscalautist"
    password = "8620RIB53!"
  }
}
