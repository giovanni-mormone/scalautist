package view.fxview.component.driver.subcomponent.parent

trait SalaryBoxParent {
  /**
   * method that allow return information for specific salary
   * @param idSalary id of the salary that driver want view
   */
  def infoSalary(idSalary:Int):Unit
}
